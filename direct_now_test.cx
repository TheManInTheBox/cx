// Simple direct test of now() function
on system.start (event)
{
    print("🚀 Testing now() function directly");
    
    var timestamp = now();
    print("📅 Current timestamp: " + timestamp);
    
    print("✅ now() function test complete");
}
