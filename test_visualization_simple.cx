conscious SimpleTestAgent {
    realize(self: object) {
        learn self;
        
        // Generate some simple test events
        emit simple.test.start {
            message: "Simple visualization test starting"
        };
        
        emit agent.heartbeat {
            agent: "SimpleTestAgent",
            status: "alive"
        };
        
        emit simple.test.end {
            message: "Simple visualization test complete"
        };
    }
    
    on simple.test.start(event) {
        print("🚀 Test starting:");
        print(event);
    }
    
    on agent.heartbeat(event) {
        print("💓 Agent heartbeat:");
        print(event);
    }
    
    on simple.test.end(event) {
        print("✅ Test complete:");
        print(event);
    }
}

// Create the test agent
var testAgent = new SimpleTestAgent();
