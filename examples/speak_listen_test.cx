// Test speak and listen functionality with Azure Realtime API
print("🎤 Testing Speak and Listen Integration with Azure Realtime API");

class VoiceTest
{
    name: string = "VoiceTest";
    
    function startVoiceSession()
    {
        print("🎧 Starting voice session...");
        
        // Connect to Azure Realtime API first
        emit realtime.connect { demo: "voice_test" };
    }
    
    function sendVoiceMessage(text: string)
    {
        print("🔊 Sending voice message: " + text);
        
        // Send text to Azure Realtime API for voice synthesis
        emit realtime.text.send { 
            text: text,
            deployment: "gpt-4o-mini-realtime-preview"
        };
    }
    
    on realtime.connected (event)
    {
        print("✅ Azure Realtime connected - creating session");
        emit realtime.session.create { 
            deployment: "gpt-4o-mini-realtime-preview",
            test: "voice"
        };
    }
    
    on realtime.session.created (event)
    {
        print("✅ Voice session created - ready for voice input/output");
        this.sendVoiceMessage("Hello, this is a test of the voice system.");
    }
    
    on realtime.text.response (event)
    {
        print("✅ Voice response received: " + event.content);
        print("  Complete: " + event.isComplete);
        
        if (event.isComplete)
        {
            print("🎉 Voice test successful!");
        }
    }
    
    on realtime.audio.response (event)
    {
        // ✅ FIXED: Safe audio data handling without .length property access
        if (event.audioData != null)
        {
            print("🔊 Audio response received - data available");
            print("📊 Audio data type: " + typeof(event.audioData));
        }
        else
        {
            print("🔊 Audio response received - no data");
        }
        
        if (event.isComplete)
        {
            print("🎵 Audio synthesis complete!");
        }
    }
}

var voiceTest = new VoiceTest();
voiceTest.startVoiceSession();

print("🎉 Voice system test initiated!");
