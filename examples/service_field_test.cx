class TestClass
{
    uses textGen from Cx.AI.TextGeneration;

    function testServiceFieldAccess()
    {
        // Just try to access the field directly first
        var serviceInstance = this.textGen;
        print("Service instance: " + serviceInstance);
    }
}

var test = new TestClass();
test.testServiceFieldAccess();
