class TestClass
{
    uses textGen from Cx.AI.TextGeneration;

    function testServicePropertyAccess()
    {
        // Try to access a property on the service
        print("About to access ServiceName property...");
        var serviceName = this.textGen.ServiceName;
        print("Service name: " + serviceName);
    }
}

var test = new TestClass();
test.testServicePropertyAccess();
