// Test case with class and AI service to isolate the issue
class TestClass
{
    uses textGen from Cx.AI.TextGeneration;
    
    function testMethod()
    {
        try
        {
            var response = await textGen.GenerateAsync("Test message");
            print("Response: " + response);
        }
        catch (error)
        {
            print("Error: " + error);
        }
    }
}

var test = new TestClass();
test.testMethod();
