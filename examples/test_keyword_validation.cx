// Test case to validate keyword rejection in object literals
class TestKeywordValidation
{
    constructor()
    {
        print("🧪 Testing keyword validation");
    }
}

print("🚀 Keyword Validation Test");

// This should work - no keywords as property names
var validObject = {
    name: "test",
    value: 123,
    agent: "agent is no longer a reserved keyword!",
    "class": "string literals can contain keywords"
};

print("✅ Valid object with 'agent' property created successfully");

// This should FAIL - using keyword 'class' as identifier property name
var invalidObject = {
    class: "this should be rejected",
    name: "test"
};

print("❌ This should not be reached if validation works");
