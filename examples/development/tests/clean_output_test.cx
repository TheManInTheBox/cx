// Simple test to verify clean output after debug logging removal
var executionStartTime = now();

conscious TestAgent
{
    realize(self: conscious)
    {
        learn self;
        emit agent.initialized { name: self.name, time: executionStartTime };
    }
    
    on system.start (event)
    {
        print("🎯 Clean Output Test Started");
        print("Agent: " + event.name);
        
        emit test.complete { status: "success", agent: event.name };
    }
    
    on test.complete (event)
    {
        print("✅ Clean Output Test Complete");
        print("Status: " + event.status);
        print("Agent: " + event.agent);
        
        emit system.shutdown { reason: "test_complete" };
    }
}

var testAgent = new TestAgent({ name: "CleanOutputAgent" });

emit system.start { name: "CleanOutputAgent" };
