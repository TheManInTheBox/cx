// CX Language - Agent Keyword Test
// Testing the new agent syntax: agent ClassName(params)

// Test Class for Agent Creation
class TestAgent  
{
    name: string;
    
    constructor(agentName)
    {
        this.name = agentName;
        print("Agent created: " + agentName);
    }
    
    function greet()
    {
        print("Hello from agent: " + this.name);
    }
}

// Test the new agent syntax
print("Testing agent keyword syntax");

// Test basic agent creation with the new agent keyword
var agent1 = agent TestAgent("Alpha");
print("Agent instantiation test complete");

print("Agent keyword grammar test complete");
