// Test script: Generate an image, then analyze it (image-to-text)

// Generate an image using a prompt
var imageResponse = task("Generate an image of a cat sitting on a laptop. Return only the direct image URL as 'ImageUrl'.");
print("Image URL: " + imageResponse.ImageUrl);

// Analyze the generated image (image-to-text)
var analysis = analyzeImage(imageResponse.ImageUrl);
print("Image Description: " + analysis.Description);
print("Extracted Text: " + analysis.ExtractedText);
print("Tags: " + analysis.Tags);
print("Objects: " + analysis.Objects);
