// Simple test to debug float literal support
// Testing different numeric formats

print("=== Float Literal Debug Test ===");

// Integer literals (should work)
var intValue = 42;
print("Integer: " + intValue);

// Float literals (currently failing)
try 
{
    var floatValue = 19.99;
    print("Float: " + floatValue);
}
catch (error)
{
    print("Float error: " + error);
}

// Alternative float syntax
try 
{
    var altFloat = 3.14159;
    print("Alt float: " + altFloat);
}
catch (error)
{
    print("Alt float error: " + error);
}

print("=== Test Complete ===");
