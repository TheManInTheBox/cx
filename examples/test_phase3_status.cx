// Testing current Phase 3 functionality
// Based on conversation summary - checking what's working

print("=== Phase 3 Status Test ===");

// Test 1: Basic functions (Phase 2 - should work)
function greetPerson(name) 
{
    print("Hello " + name);
}

greetPerson("World");

// Test 2: Variables and expressions (Phase 1 - should work)
var x = 10;
var y = 20;
var sum = x + y;
print("Sum: " + sum);

// Test 3: Control flow (Phase 1 - should work) 
if (x < y) 
{
    print("x is less than y");
}

// Test 4: Simple class (Phase 3 - should work now!)
class SimpleClass 
{
    constructor(value) 
    {
        this.value = value;
    }
    
    function getValue() 
    {
        return this.value;
    }
}

// Test the class
var obj = new SimpleClass(42);
print("Class created with value: " + obj.getValue());

// Test 5: Arrays (Phase 3 - should work now!)
var numbers = [1, 2, 3, 4, 5];
print("Array created with " + numbers.length + " elements");

// Test 6: For-in loops (Phase 3 - should work now!)
for (var num in numbers) 
{
    print("Number: " + num);
}

// Test 7: Exception handling (Phase 3 - should work now!)
try 
{
    var result = 10 / 0;
    print("Result: " + result);
}
catch (e) 
{
    print("Caught exception: " + e);
}

// Test 8: Function return values (Phase 3 - should work now!)
function addNumbers(a, b) 
{
    return a + b;
}

var result = addNumbers(5, 3);
print("Function returned: " + result);

print("=== Status Test Complete ===");
