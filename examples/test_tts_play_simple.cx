// Test for the new SpeakAsync method - let's hear it speak! (Fixed version)
using textToSpeech from "Cx.AI.TextToSpeech";

print("🎵 Testing TTS SpeakAsync - Let's hear it speak!");
print("================================================");
print("");

try 
{
    print("🎤 Playing Phase 4 celebration message...");
    print("   (Audio should start playing automatically)");
    print("");
    
    var playResult = textToSpeech.SpeakAsync("Congratulations! Phase 4 multi-service configuration is complete and working perfectly! This audio should now play automatically on your speakers!");
    
    print("✅ SpeakAsync executed successfully!");
    print("   🔊 Audio should be playing now");
    print("   📁 Check your system's default audio player");
    print("");
    print("🎉 If you heard the celebration message, TTS playback is working!");
}
catch (error)
{
    print("❌ Error: " + error);
}

print("");
print("🚀 Phase 4 TTS with automatic playback complete!");
