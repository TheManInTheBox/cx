print("=== CX LANGUAGE FULL FEATURE TEST ===");

// Variable declarations and assignments
var name = "Aura CX";
var version = 4.1;
var isActive = true;

print("Language: " + name);
print("Version: " + version);

// Arithmetic operations
var result = 10 + 5 * 2;
print("Calculation result: " + result);

// Control flow - if statements
if (result > 15)
{
    print("Result is greater than 15");
}
else
{
    print("Result is 15 or less");
}

// Arrays and for-in loops
var features = ["Variables", "Functions", "Classes", "AI Services", "TTS"];

print("CX Language Features:");
for (var feature in features)
{
    print("- " + feature);
}

// Functions
function calculateScore(base, multiplier)
{
    return base * multiplier + 100;
}

var score = calculateScore(50, 2);
print("Calculated score: " + score);

// Exception handling
try
{
    print("Testing exception handling...");
    var testValue = 42;
    print("Exception test passed: " + testValue);
}
catch (error)
{
    print("Error caught: " + error);
}

// Object literals
var config = {
    temperature: 0.8,
    maxTokens: 500,
    model: "gpt-4"
};

print("Config temperature: " + config.temperature);

print("=== ALL CORE FEATURES WORKING ===");
