// Test while loop with minimal complexity to isolate IL issue
print("=== While Loop IL Debug Test ===");

var counter = 0;
print("Initial counter: " + counter);

while (counter < 3)
{
    print("Loop iteration: " + counter);
    counter = counter + 1;
}

print("Final counter: " + counter);
print("=== Test Complete ===");
