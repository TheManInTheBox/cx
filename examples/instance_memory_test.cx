// Test instance-specific memory collections for agents
// Each agent should get their own unique Kernel Memory collection for cognitive operations

class TestAgent
{
    name: string;
    
    constructor(agentName: string)
    {
        this.name = agentName;
        print("🤖 " + this.name + " agent created");
    }
    
    function testThinking()
    {
        print("🧠 " + this.name + " starting think test");
        
        think {
            prompt: "What is my name and remember this for future reference?",
            handlers: [ thinking.complete ]
        };
    }
    
    on thinking.complete (event)
    {
        print("✅ " + this.name + " thinking complete!");
        print("   Prompt: " + event.prompt);
        print("   Result: " + event.result);
        print("   Instance Collection: " + event.instanceCollection);
        print("");
    }
}

// Create multiple agents to test instance-specific memory
var agent1 = new TestAgent("Alice");
var agent2 = new TestAgent("Bob");

print("🔬 Testing instance-specific memory collections");
print("Each agent should use a different Kernel Memory collection");
print("");

// Test thinking for both agents
agent1.testThinking();
agent2.testThinking();

print("✨ Test complete - check logs for instance collection names");
