// POWERSHELLPHI BASIC TEST - DIRECT CONSCIOUS SCOPE VALIDATION
// Tests: PowerShellPhi integration with conscious entity in minimal scope

on system.start (event)
{
    print("PowerShellPhi Basic Test");
    print("========================");
    print("Creating conscious agent...");
    
    // Create simple agent without constructor parameters
    var agent = new PhiTestAgent();
    emit test.start;
}

// Minimal conscious entity for PowerShellPhi testing
conscious PhiTestAgent 
{
    realize(self: conscious)
    {
        print("PhiTestAgent conscious entity activated");
        emit agent.activated;
    }
    
    on test.start (event)
    {
        print("Starting PowerShellPhi test in conscious scope...");
        
        // Direct think operation within conscious scope
        think { 
            prompt: "Hello phi-3, what is 1+1? Answer with just the number."
        };
    }
    
    on thinking.complete (event)
    {
        print("✅ PowerShellPhi thinking completed!");
        print("Result: " + event.result);
        emit system.shutdown { reason: "test_success" };
    }
}
