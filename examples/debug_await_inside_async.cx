// Test to isolate the await-inside-async-method issue

print("🎯 Debug: Testing await inside async method");

class TestAgent
{
    async function withAwait()
    {
        print("Before await");
        var result = await this.ThinkAsync("test");
        print("After await");
        return "completed";
    }
}

print("Creating test agent...");
var agent = new TestAgent();

print("Calling withAwait method...");
var result = await agent.withAwait();

print("Final result: " + result);
print("✅ Test complete");
