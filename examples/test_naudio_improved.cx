// Test improved NAudio direct memory playback
using textToSpeech from "Cx.AI.TextToSpeech";

print("🔊 Testing Improved NAudio Memory Playback");
print("==========================================");
print("");

try 
{
    print("🎵 Testing NAudio with improved WAV validation...");
    print("   (Pure memory streaming with header validation)");
    print("");
    
    textToSpeech.SpeakAsync("NAudio memory playback test! This should now work perfectly with proper WAV header validation and memory stream positioning.");
    
    print("✅ Improved NAudio playback executed!");
    print("   🎯 WAV header validation added");
    print("   📍 Memory stream position reset");
    print("   🚀 Should work with pure NAudio now!");
}
catch (error)
{
    print("❌ Error: " + error);
}

print("");
print("🎉 Improved NAudio test complete!");
