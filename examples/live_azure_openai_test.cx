// Live Azure OpenAI Phase 4 Test
// This test demonstrates all 7 AI functions calling live Azure OpenAI service

print("===============================================");
print("CX LANGUAGE - LIVE AZURE OPENAI PHASE 4 TEST");
print("Testing All 7 Core AI Functions with Live API");
print("===============================================");
print("");

// Test 1: task() - AI-powered task planning
print("1. Testing task() function with live Azure OpenAI...");
try
{
    var taskResult = task("Create a simple bubble sort algorithm");
    print("   Live AI Result: " + taskResult);
}
catch (error)
{
    print("   Error in task(): " + error);
}
print("");

// Test 2: reason() - AI-powered reasoning
print("2. Testing reason() function with live Azure OpenAI...");
try
{
    var reasonResult = reason("Why is recursion important in programming?");
    print("   Live AI Result: " + reasonResult);
}
catch (error)
{
    print("   Error in reason(): " + error);
}
print("");

// Test 3: synthesize() - AI-powered code synthesis
print("3. Testing synthesize() function with live Azure OpenAI...");
try
{
    var synthesizeResult = synthesize("Write a JavaScript function to reverse a string");
    print("   Live AI Result: " + synthesizeResult);
}
catch (error)
{
    print("   Error in synthesize(): " + error);
}
print("");

// Test 4: process() - AI-powered data processing
print("4. Testing process() function with live Azure OpenAI...");
try
{
    var processResult = process("Analyze the performance of this algorithm", "O(n^2) bubble sort");
    print("   Live AI Result: " + processResult);
}
catch (error)
{
    print("   Error in process(): " + error);
}
print("");

// Test 5: generate() - AI-powered content generation
print("5. Testing generate() function with live Azure OpenAI...");
try
{
    var generateResult = generate("Write a brief explanation of machine learning");
    print("   Live AI Result: " + generateResult);
}
catch (error)
{
    print("   Error in generate(): " + error);
}
print("");

// Test 6: embed() - AI-powered vector embeddings
print("6. Testing embed() function with live Azure OpenAI...");
try
{
    var embedResult = embed("Convert this text to vector embeddings");
    print("   Live AI Result: " + embedResult);
}
catch (error)
{
    print("   Error in embed(): " + error);
}
print("");

// Test 7: adapt() - AI-powered self-optimization
print("7. Testing adapt() function with live Azure OpenAI...");
try
{
    var adaptResult = adapt("function inefficient() { for(let i=0; i<1000; i++) { console.log(i); } }");
    print("   Live AI Result: " + adaptResult);
}
catch (error)
{
    print("   Error in adapt(): " + error);
}
print("");

print("===============================================");
print("LIVE AZURE OPENAI PHASE 4 TEST COMPLETE");
print("All 7 core AI functions tested with live API");
print("===============================================");
