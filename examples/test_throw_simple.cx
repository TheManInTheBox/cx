// Simple throw test without string concatenation
print("Before throw");

try {
    throw "Test exception";
} catch (error) {
    print("Caught exception");
    print(error);
}

print("After throw");
