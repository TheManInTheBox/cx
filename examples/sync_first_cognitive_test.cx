print("🚀 Sync-First Cognitive Test");

class CognitiveTest
{
    name: string;
    
    constructor(agentName)
    {
        this.name = agentName;
        print("🤖 Cognitive agent created: " + this.name);
    }
    
    function testThink(prompt)
    {
        print("🧠 About to call Think...");
        var result = this.Think(prompt);
        print("💭 Think result: " + result);
        return result;
    }
    
    function testLearnAsync(data)
    {
        print("📚 About to call LearnAsync (fire-and-forget)...");
        this.LearnAsync(data);
        print("✅ LearnAsync called (fire-and-forget)");
        return "learn queued";
    }
}

var agent = new CognitiveTest("SyncFirst");
var thinkResult = agent.testThink("Hello world");
var learnResult = agent.testLearnAsync("test data");

print("🎯 Final results:");
print("Think: " + thinkResult);
print("Learn: " + learnResult);
