print("🔧 Simple Task Test - Direct Call without Variable Assignment");

class TaskAgent
{
    name: string;
    
    constructor(agentName)
    {
        this.name = agentName;
        print("🤖 Agent " + this.name + " ready");
    }
    
    function directTaskTest()
    {
        print("📡 Testing direct task call...");
        
        // Direct call should return Task - don't store in variable yet
        this.Learn("test_data_direct");
        
        print("✅ Direct call completed");
        return "direct_test_complete";
    }
}

var agent = new TaskAgent("SimpleTest");
var result = agent.directTaskTest();
print("🎯 Result: " + result);
