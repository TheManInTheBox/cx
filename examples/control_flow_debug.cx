// Simple control flow test to isolate the issue
print("=== Control Flow Debug Test ===");

// Test variable declaration and access
var counter = 0;
print("Initial counter: " + counter);

// Test simple if statement (known to work)
if (counter == 0)
{
    print("Counter is zero");
}

// Test assignment
counter = 5;
print("After assignment: " + counter);

print("=== Test Complete ===");
