class TestClass
{
    constructor(config)
    {
        print("Constructor called!");
    }
}

try
{
    print("About to create object...");
    var obj = new TestClass({ name: "test" });
    print("Object created successfully!");
}
catch (error)
{
    print("Error during object creation: " + error);
}
