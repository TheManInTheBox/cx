// Simple test to demonstrate successful function injection with correct parameter naming

print("=== Testing Function Injection Success ===");

// Test 1: Generate a simple function with correct parameter naming
print("\n--- Test 1: Simple Function ---");
var result1 = adapt("Create a function called 'multiply' that takes two parameters a and b, multiplies them together and returns the result");
print("Result: " + result1);

// Test 2: Generate a function that calls another function
print("\n--- Test 2: Function with Call ---");
var result2 = adapt("Create a function called 'calculate' that takes a parameter n, calls multiply(n, 2) and prints the result");
print("Result: " + result2);

// Test 3: Try to call the injected functions
print("\n--- Test 3: Calling Injected Functions ---");
try 
{
    var multiplyResult = multiply(6, 7);
    print("Multiply result: " + multiplyResult);
}
catch (error)
{
    print("Error calling multiply: " + error);
}

try 
{
    var calculateResult = calculate(5);
    print("Calculate result: " + calculateResult);
}
catch (error)
{
    print("Error calling calculate: " + error);
}

print("\n=== Test Complete ===");
