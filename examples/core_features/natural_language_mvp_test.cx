// Natural Language MVP Test - Behavior Flow Demo
// Demonstrates: User input → AI inference → Prompt for more info if needed

on system.start (event)
{
    print("🧠 Natural Language MVP Test - Behavior Flow");
    print("=================================================");
    print("This test demonstrates the MVP behavior flow:");
    print("1. User inputs natural language");
    print("2. Text is sent for AI inference (think service)");
    print("3. AI responds with result or asks for more info");
    print("4. Terminal prompts for additional details if needed");
    print("");
    
    // Start the developer terminal for natural language input
    emit terminal.start;
}

// Test handler to verify the think service is working
on ai.think.response (event)
{
    print("✅ AI Think Response Test:");
    print("  Original Prompt: " + event.originalPrompt);
    print("  AI Result: " + event.result);
    print("");
}

on ai.think.needs_more_info (event) 
{
    print("🤔 AI Needs More Info Test:");
    print("  Question: " + event.question);
    print("  Original Prompt: " + event.originalPrompt);
    print("");
}
