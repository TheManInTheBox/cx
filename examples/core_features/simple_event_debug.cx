// Simple Event Debug Test
print("=== Event Debug Test ===");

// Simple global handler
on test.event (payload) {
    print("✅ TEST EVENT RECEIVED: " + payload.message);
}

print("Handler registered, now emitting event...");

// Emit the test event
emit test.event, { message: "Hello World" };

print("Event emitted, waiting...");
print("If you see this but no '✅ TEST EVENT RECEIVED', then events aren't firing.");
