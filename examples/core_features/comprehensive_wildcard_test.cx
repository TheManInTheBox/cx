// Comprehensive Wildcard Pattern Test

print("=== Comprehensive Wildcard Pattern Test ===");

// Wildcard handler that should catch multiple events
on user.any.input (payload)
{
    print("🎯 WILDCARD: user.any.input caught - " + payload.message);
}

// Specific handler
on user.chat.message (payload)
{
    print("📝 SPECIFIC: user.chat.message - " + payload.text);
}

print("\n🧪 TEST: Emit user.chat.message");
print("Expected: BOTH wildcard AND specific handlers should fire");
emit user.chat.message, { text: "Hello from chat!", message: "Should trigger wildcard too" };

print("\n=== Results should show BOTH handlers firing ===");
