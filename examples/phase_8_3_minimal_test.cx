// Phase 8.3 - Minimal test to isolate InvalidProgramException
class TestAgent
{
    uses textGen from Cx.AI.TextGeneration;
    
    constructor()
    {
        print("🧪 Minimal test agent initialized");
    }
    
    on system.test (payload)
    {
        print("📡 Test event received");
    }
}

print("🚀 Phase 8.3 Minimal Test Demo");

var testAgent = new TestAgent();

print("✅ Agent created successfully");

emit system.test, { message: "test" };

print("✅ Event emitted successfully");
