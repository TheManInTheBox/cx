// Ultra Simple Execute Test - No Complex Array Access
class SimpleTestAgent 
{
    name: string;
    
    constructor(agentName)
    {
        this.name = agentName;
        print("🧪 Created simple test agent: " + this.name);
    }
    
    function runTest()
    {
        print("🚀 Running simple test...");
        this.Execute("Write-Output 'Hello'");
        print("✅ Command executed");
    }
    
    // Very simple event handler - no array access or complex operations
    on command.executed (payload)
    {
        print("🎉 Event received!");
        print("Command executed successfully");
    }
}

print("🔬 Ultra Simple Execute Test");
print("============================");

try
{
    var agent = new SimpleTestAgent("UltraSimple");
    agent.runTest();
    
    // Wait mechanism
    agent.Execute("Start-Sleep -Seconds 5; Write-Output 'Done'");
    
    print("✅ Test complete!");
}
catch (error)
{
    print("❌ Error: " + error.message);
}
