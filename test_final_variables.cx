function main() {
    x = 42
    y = "Hello"
    
    print("Initial values:")
    print(x)
    print(y)
    
    // Note: Our current language treats assignments as declarations
    // So this creates new variables rather than reassigning
    // That's a language design issue, not a compiler bug
}
